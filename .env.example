# Server Configuration
PORT=3000
NODE_ENV=development

# PostgreSQL Configuration
# Option 1: Individual configuration (for local development)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=foodapp
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password
POSTGRES_SSL=false

# Option 2: Connection string (for production, e.g., Render)
# DATABASE_URL=postgresql://user:password@host:port/database

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/foodapp_notifications
# For production (e.g., MongoDB Atlas):
# MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/foodapp_notifications?retryWrites=true&w=majority

# Add more environment variables as needed
# JWT_SECRET=your-secret-key
# CORS_ORIGIN=http://localhost:3000
